File handling means reading, writing, updating, and deleting files stored in your system.
In Node.js, file handling is done using the fs (File System) module — a built-in module, so you don’t have to install anything.
appended text.
